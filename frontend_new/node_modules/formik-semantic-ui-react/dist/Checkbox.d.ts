/// <reference types="react" />
import { CheckboxProps as _CheckboxProps } from 'semantic-ui-react';
import { FieldErrorProps, FieldProps } from './types';
export interface CheckboxProps extends FieldProps, _CheckboxProps, FieldErrorProps {
    name: string;
}
export declare const Checkbox: ({ name, validate, fast, onChange: _onChange, errorPrompt, ...restProps }: CheckboxProps) => JSX.Element;
export default Checkbox;
